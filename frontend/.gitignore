# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment. VITE_ values are public, but keep them out of the repo.
.env
.env.*
!.env.example

# The production API URL is public and belongs in the repo, so a build never
# depends on someone remembering to set it in a dashboard.
!.env.production
