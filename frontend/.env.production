# Read automatically by `vite build`, so a production build always points at
# the real API without anyone having to remember a dashboard setting.
#
# Safe to commit: this is a public URL that ends up in the JavaScript bundle
# regardless. Nothing secret may ever go in a VITE_ variable.
VITE_API_URL=https://memories-yujexbjt.on-forge.com
